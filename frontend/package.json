{
  "name": "doctor-search-frontend",
  "version": "1.0.0",
  "description": "HTML/JavaScript frontend for Doctor Search application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-deps": "npm install",
    "build": "echo 'No build step required for static HTML/JS/CSS'"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 